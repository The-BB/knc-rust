--- a/Cargo.toml
+++ b/Cargo.toml
@@ -11,9 +11,9 @@ license = "MIT"
 
 [dependencies]
 # nushell dependencies
-nu-plugin = "0.104.0"
-nu-protocol = "0.104.0"
-nu-path = "0.104.0"
+nu-plugin = "0.104.1"
+nu-protocol = "0.104.1"
+nu-path = "0.104.1"
 
 # for local development, you can use a path dependency
 # nu-plugin = { path = "../nushell/crates/nu-plugin", version = "0.98.0" }
@@ -23,8 +23,10 @@ nu-path = "0.104.0"
 fancy-regex = "0.14.0"
 
 [dev-dependencies]
-nu-plugin-test-support = "0.104.0"
+nu-plugin-test-support = "0.104.1"
 # nu-plugin-test-support = { path = "../nushell/crates/nu-plugin-test-support" }
+# error: failed to select a version for `chrono`.
+chrono = "=0.4.40"
 
 [profile.release]
 opt-level = "s"     # Optimize for size
@@ -33,3 +35,14 @@ lto = "thin"
 
 [profile.dev]
 opt-level = 0
+
+[patch.crates-io]
+nu-path = { path = "../nushell-0.104.1/crates/nu-path" }
+nu-plugin = { path = "../nushell-0.104.1/crates/nu-plugin" }
+nu-plugin-test-support = { path = "../nushell-0.104.1/crates/nu-plugin-test-support" }
+nu-protocol = { path = "../nushell-0.104.1/crates/nu-protocol" }
+# localtime/zoneinfo
+chrono = { path = "../chrono-0.4.40" }
+iana-time-zone = { path = "../iana-time-zone-0.1.61" }
+# all: no SYS_getrandom
+getrandom = { path = "../getrandom-0.2.15" }
