--- a/spider_agent/src/automation/browser.rs
+++ b/spider_agent/src/automation/browser.rs
@@ -5392,7 +5392,7 @@ pub async fn run_spawn_pages_with_option
     use chromiumoxide::cdp::browser_protocol::network::EventDataReceived;
     use dashmap::DashMap;
     use futures::StreamExt;
-    use std::sync::atomic::{AtomicU64, Ordering};
+    use std::sync::atomic::{AtomicUsize, Ordering};
     use std::sync::Arc;
 
     if urls.is_empty() {
@@ -5448,7 +5448,7 @@ pub async fn run_spawn_pages_with_option
             options.page_wait.apply(&new_page).await;
 
             // Set up bytes tracking if enabled
-            let total_bytes: Arc<AtomicU64> = Arc::new(AtomicU64::new(0));
+            let total_bytes: Arc<AtomicUsize> = Arc::new(AtomicUsize::new(0));
             let response_map: Arc<DashMap<String, u64>> = Arc::new(DashMap::new());
 
             let bytes_listener = if options.track_bytes {
--- a/spider_agent/src/config.rs
+++ b/spider_agent/src/config.rs
@@ -3,7 +3,7 @@
 use dashmap::DashMap;
 use serde::{Deserialize, Serialize};
 use std::collections::HashMap;
-use std::sync::atomic::{AtomicU64, Ordering};
+use std::sync::atomic::{AtomicUsize, Ordering};
 use std::time::Duration;
 
 /// Usage limits for controlling agent resource consumption.
@@ -499,34 +499,34 @@ impl ResearchOptions {
 #[derive(Debug)]
 pub struct UsageStats {
     /// Total LLM prompt tokens used.
-    pub prompt_tokens: AtomicU64,
+    pub prompt_tokens: AtomicUsize,
     /// Total LLM completion tokens used.
-    pub completion_tokens: AtomicU64,
+    pub completion_tokens: AtomicUsize,
     /// Total LLM calls made.
-    pub llm_calls: AtomicU64,
+    pub llm_calls: AtomicUsize,
     /// Total search calls made.
-    pub search_calls: AtomicU64,
+    pub search_calls: AtomicUsize,
     /// Total HTTP fetch calls made.
-    pub fetch_calls: AtomicU64,
+    pub fetch_calls: AtomicUsize,
     /// Total web browser calls made (Chrome/WebDriver combined).
-    pub webbrowser_calls: AtomicU64,
+    pub webbrowser_calls: AtomicUsize,
     /// Custom tool calls tracked by tool name (lock-free via DashMap).
-    pub custom_tool_calls: DashMap<String, AtomicU64>,
+    pub custom_tool_calls: DashMap<String, AtomicUsize>,
     /// Total tool calls made.
-    pub tool_calls: AtomicU64,
+    pub tool_calls: AtomicUsize,
 }
 
 impl Default for UsageStats {
     fn default() -> Self {
         Self {
-            prompt_tokens: AtomicU64::new(0),
-            completion_tokens: AtomicU64::new(0),
-            llm_calls: AtomicU64::new(0),
-            search_calls: AtomicU64::new(0),
-            fetch_calls: AtomicU64::new(0),
-            webbrowser_calls: AtomicU64::new(0),
+            prompt_tokens: AtomicUsize::new(0),
+            completion_tokens: AtomicUsize::new(0),
+            llm_calls: AtomicUsize::new(0),
+            search_calls: AtomicUsize::new(0),
+            fetch_calls: AtomicUsize::new(0),
+            webbrowser_calls: AtomicUsize::new(0),
             custom_tool_calls: DashMap::new(),
-            tool_calls: AtomicU64::new(0),
+            tool_calls: AtomicUsize::new(0),
         }
     }
 }
@@ -539,9 +539,9 @@ impl UsageStats {
 
     /// Add tokens from an LLM response.
     pub fn add_tokens(&self, prompt: u64, completion: u64) {
-        self.prompt_tokens.fetch_add(prompt, Ordering::Relaxed);
+        self.prompt_tokens.fetch_add(prompt.try_into().unwrap(), Ordering::Relaxed);
         self.completion_tokens
-            .fetch_add(completion, Ordering::Relaxed);
+            .fetch_add(completion.try_into().unwrap(), Ordering::Relaxed);
     }
 
     /// Increment LLM call count.
@@ -568,7 +568,7 @@ impl UsageStats {
     pub fn increment_custom_tool_calls(&self, tool_name: &str) {
         self.custom_tool_calls
             .entry(tool_name.to_string())
-            .or_insert_with(|| AtomicU64::new(0))
+            .or_insert_with(|| AtomicUsize::new(0))
             .fetch_add(1, Ordering::Relaxed);
     }
 
@@ -577,11 +577,11 @@ impl UsageStats {
         self.custom_tool_calls
             .get(tool_name)
             .map(|v| v.load(Ordering::Relaxed))
-            .unwrap_or(0)
+            .unwrap_or(0).try_into().unwrap()
     }
 
     /// Get total custom tool calls across all tools.
-    pub fn total_custom_tool_calls(&self) -> u64 {
+    pub fn total_custom_tool_calls(&self) -> usize {
         self.custom_tool_calls
             .iter()
             .map(|entry| entry.value().load(Ordering::Relaxed))
@@ -595,7 +595,7 @@ impl UsageStats {
 
     /// Get total tokens used.
     pub fn total_tokens(&self) -> u64 {
-        self.prompt_tokens.load(Ordering::Relaxed) + self.completion_tokens.load(Ordering::Relaxed)
+        (self.prompt_tokens.load(Ordering::Relaxed) + self.completion_tokens.load(Ordering::Relaxed)).try_into().unwrap()
     }
 
     /// Get a snapshot of all stats.
@@ -603,18 +603,18 @@ impl UsageStats {
         let custom_tool_calls: HashMap<String, u64> = self
             .custom_tool_calls
             .iter()
-            .map(|entry| (entry.key().clone(), entry.value().load(Ordering::Relaxed)))
+            .map(|entry| (entry.key().clone(), entry.value().load(Ordering::Relaxed).try_into().unwrap()))
             .collect();
 
         UsageSnapshot {
-            prompt_tokens: self.prompt_tokens.load(Ordering::Relaxed),
-            completion_tokens: self.completion_tokens.load(Ordering::Relaxed),
-            llm_calls: self.llm_calls.load(Ordering::Relaxed),
-            search_calls: self.search_calls.load(Ordering::Relaxed),
-            fetch_calls: self.fetch_calls.load(Ordering::Relaxed),
-            webbrowser_calls: self.webbrowser_calls.load(Ordering::Relaxed),
-            custom_tool_calls,
-            tool_calls: self.tool_calls.load(Ordering::Relaxed),
+            prompt_tokens: (self.prompt_tokens.load(Ordering::Relaxed)).try_into().unwrap(),
+            completion_tokens: (self.completion_tokens.load(Ordering::Relaxed)).try_into().unwrap(),
+            llm_calls: (self.llm_calls.load(Ordering::Relaxed)).try_into().unwrap(),
+            search_calls: (self.search_calls.load(Ordering::Relaxed)).try_into().unwrap(),
+            fetch_calls: (self.fetch_calls.load(Ordering::Relaxed)).try_into().unwrap(),
+            webbrowser_calls: (self.webbrowser_calls.load(Ordering::Relaxed)).try_into().unwrap(),
+            custom_tool_calls: custom_tool_calls.try_into().unwrap(),
+            tool_calls: (self.tool_calls.load(Ordering::Relaxed)).try_into().unwrap(),
         }
     }
 
@@ -636,8 +636,8 @@ impl UsageStats {
     pub fn check_llm_limit(&self, limits: &UsageLimits) -> Option<LimitType> {
         if let Some(limit) = limits.max_llm_calls {
             let used = self.llm_calls.load(Ordering::Relaxed);
-            if used >= limit {
-                return Some(LimitType::LlmCalls { used, limit });
+            if used >= limit.try_into().unwrap() {
+                return Some(LimitType::LlmCalls { used: used.try_into().unwrap(), limit });
             }
         }
         None
@@ -647,8 +647,8 @@ impl UsageStats {
     pub fn check_search_limit(&self, limits: &UsageLimits) -> Option<LimitType> {
         if let Some(limit) = limits.max_search_calls {
             let used = self.search_calls.load(Ordering::Relaxed);
-            if used >= limit {
-                return Some(LimitType::SearchCalls { used, limit });
+            if used >= limit.try_into().unwrap() {
+                return Some(LimitType::SearchCalls { used: used.try_into().unwrap(), limit });
             }
         }
         None
@@ -658,8 +658,8 @@ impl UsageStats {
     pub fn check_fetch_limit(&self, limits: &UsageLimits) -> Option<LimitType> {
         if let Some(limit) = limits.max_fetch_calls {
             let used = self.fetch_calls.load(Ordering::Relaxed);
-            if used >= limit {
-                return Some(LimitType::FetchCalls { used, limit });
+            if used >= limit.try_into().unwrap() {
+                return Some(LimitType::FetchCalls { used: used.try_into().unwrap(), limit });
             }
         }
         None
@@ -669,8 +669,8 @@ impl UsageStats {
     pub fn check_webbrowser_limit(&self, limits: &UsageLimits) -> Option<LimitType> {
         if let Some(limit) = limits.max_webbrowser_calls {
             let used = self.webbrowser_calls.load(Ordering::Relaxed);
-            if used >= limit {
-                return Some(LimitType::WebbrowserCalls { used, limit });
+            if used >= limit.try_into().unwrap() {
+                return Some(LimitType::WebbrowserCalls { used: used.try_into().unwrap(), limit });
             }
         }
         None
@@ -680,8 +680,8 @@ impl UsageStats {
     pub fn check_custom_tool_limit(&self, limits: &UsageLimits) -> Option<LimitType> {
         if let Some(limit) = limits.max_custom_tool_calls {
             let used = self.total_custom_tool_calls();
-            if used >= limit {
-                return Some(LimitType::CustomToolCalls { used, limit });
+            if used >= limit.try_into().unwrap() {
+                return Some(LimitType::CustomToolCalls { used: used.try_into().unwrap(), limit });
             }
         }
         None
@@ -691,8 +691,8 @@ impl UsageStats {
     pub fn check_tool_limit(&self, limits: &UsageLimits) -> Option<LimitType> {
         if let Some(limit) = limits.max_tool_calls {
             let used = self.tool_calls.load(Ordering::Relaxed);
-            if used >= limit {
-                return Some(LimitType::ToolCalls { used, limit });
+            if used >= limit.try_into().unwrap() {
+                return Some(LimitType::ToolCalls { used: used.try_into().unwrap(), limit });
             }
         }
         None
@@ -705,24 +705,24 @@ impl UsageStats {
         let total = prompt + completion;
 
         if let Some(limit) = limits.max_total_tokens {
-            if total >= limit {
-                return Some(LimitType::TotalTokens { used: total, limit });
+            if total >= limit.try_into().unwrap() {
+                return Some(LimitType::TotalTokens { used: total.try_into().unwrap(), limit });
             }
         }
 
         if let Some(limit) = limits.max_prompt_tokens {
-            if prompt >= limit {
+            if prompt >= limit.try_into().unwrap() {
                 return Some(LimitType::PromptTokens {
-                    used: prompt,
+                    used: prompt.try_into().unwrap(),
                     limit,
                 });
             }
         }
 
         if let Some(limit) = limits.max_completion_tokens {
-            if completion >= limit {
+            if completion >= limit.try_into().unwrap() {
                 return Some(LimitType::CompletionTokens {
-                    used: completion,
+                    used: completion.try_into().unwrap(),
                     limit,
                 });
             }
--- a/spider/src/features/automation.rs
+++ b/spider/src/features/automation.rs
@@ -856,8 +856,8 @@ pub async fn process_spawn_pages_with_co
             };
 
             // Track bytes in a separate task while automation runs
-            use std::sync::atomic::{AtomicU64, Ordering};
-            let total_bytes: std::sync::Arc<AtomicU64> = std::sync::Arc::new(AtomicU64::new(0));
+            use std::sync::atomic::{AtomicUsize, Ordering};
+            let total_bytes: std::sync::Arc<AtomicUsize> = std::sync::Arc::new(AtomicUsize::new(0));
             let response_map: std::sync::Arc<dashmap::DashMap<String, u64>> =
                 std::sync::Arc::new(dashmap::DashMap::new());
 
--- a/spider/src/utils/auto_throttle.rs
+++ b/spider/src/utils/auto_throttle.rs
@@ -13,7 +13,7 @@
 mod inner {
     use crate::compact_str::CompactString;
     use dashmap::DashMap;
-    use std::sync::atomic::{AtomicU64, Ordering};
+    use std::sync::atomic::{AtomicUsize, Ordering};
     use std::time::Duration;
 
     /// Maximum tracked domains before LRU eviction.
@@ -25,25 +25,25 @@ mod inner {
     /// Per-domain latency state stored as atomic u64 bit-patterns.
     struct DomainLatency {
         /// EMA of response time in microseconds, stored as f64 bits.
-        ema_us: AtomicU64,
+        ema_us: AtomicUsize,
         /// Number of samples recorded (saturates at u64::MAX).
-        samples: AtomicU64,
+        samples: AtomicUsize,
         /// Monotonic access counter for LRU eviction.
-        last_access: AtomicU64,
+        last_access: AtomicUsize,
     }
 
     impl DomainLatency {
         fn new(access_counter: u64) -> Self {
             Self {
-                ema_us: AtomicU64::new(0),
-                samples: AtomicU64::new(0),
-                last_access: AtomicU64::new(access_counter),
+                ema_us: AtomicUsize::new(0),
+                samples: AtomicUsize::new(0),
+                last_access: AtomicUsize::new(access_counter.try_into().unwrap()),
             }
         }
 
         /// Load the current EMA in microseconds.
         fn ema_micros(&self) -> f64 {
-            f64::from_bits(self.ema_us.load(Ordering::Relaxed))
+            f64::from_bits(self.ema_us.load(Ordering::Relaxed).try_into().unwrap())
         }
 
         /// Record a new latency sample and update the EMA.
@@ -52,19 +52,19 @@ mod inner {
 
             if prev_count == 0 {
                 // First sample — seed the EMA.
-                self.ema_us.store(latency_us.to_bits(), Ordering::Relaxed);
+                self.ema_us.store(latency_us.to_bits().try_into().unwrap(), Ordering::Relaxed);
             } else {
                 // CAS loop to atomically update f64 EMA.
                 let _ = self
                     .ema_us
                     .fetch_update(Ordering::Relaxed, Ordering::Relaxed, |bits| {
-                        let old = f64::from_bits(bits);
+                        let old = f64::from_bits(bits.try_into().unwrap());
                         let new = old + alpha * (latency_us - old);
                         // Guard: never store NaN/Inf.
                         if new.is_finite() && new >= 0.0 {
-                            Some(new.to_bits())
+                            Some(new.to_bits().try_into().unwrap())
                         } else {
-                            Some(old.to_bits())
+                            Some(old.to_bits().try_into().unwrap())
                         }
                     });
             }
@@ -110,7 +110,7 @@ mod inner {
         domains: DashMap<CompactString, DomainLatency>,
         config: AutoThrottleConfig,
         /// Monotonically increasing counter for LRU.
-        access_counter: AtomicU64,
+        access_counter: AtomicUsize,
     }
 
     impl AutoThrottle {
@@ -119,7 +119,7 @@ mod inner {
             Self {
                 domains: DashMap::with_capacity(64),
                 config,
-                access_counter: AtomicU64::new(0),
+                access_counter: AtomicUsize::new(0),
             }
         }
 
@@ -143,7 +143,7 @@ mod inner {
                 entry.record(us, alpha);
             } else {
                 self.maybe_evict();
-                let entry = DomainLatency::new(counter);
+                let entry = DomainLatency::new(counter.try_into().unwrap());
                 entry.record(us, alpha);
                 self.domains.insert(key, entry);
             }
@@ -215,8 +215,8 @@ mod inner {
 
             for entry in self.domains.iter() {
                 let access = entry.value().last_access.load(Ordering::Relaxed);
-                if access < oldest_access {
-                    oldest_access = access;
+                if access < oldest_access.try_into().unwrap() {
+                    oldest_access = access.try_into().unwrap();
                     oldest_key = Some(entry.key().clone());
                 }
             }
--- a/spider/src/utils/etag_cache.rs
+++ b/spider/src/utils/etag_cache.rs
@@ -12,7 +12,7 @@
 mod inner {
     use crate::compact_str::CompactString;
     use dashmap::DashMap;
-    use std::sync::atomic::{AtomicU64, Ordering};
+    use std::sync::atomic::{AtomicUsize, Ordering};
 
     /// Maximum cached entries before LRU eviction.
     const MAX_ENTRIES: usize = 50_000;
@@ -34,7 +34,7 @@ mod inner {
     pub struct ETagCache {
         entries: DashMap<CompactString, ConditionalHeaders>,
         /// Monotonically increasing counter for LRU.
-        access_counter: AtomicU64,
+        access_counter: AtomicUsize,
     }
 
     impl ETagCache {
@@ -42,7 +42,7 @@ mod inner {
         pub fn new() -> Self {
             Self {
                 entries: DashMap::with_capacity(256),
-                access_counter: AtomicU64::new(0),
+                access_counter: AtomicUsize::new(0),
             }
         }
 
@@ -61,7 +61,7 @@ mod inner {
             let headers = ConditionalHeaders {
                 etag: etag.map(CompactString::new),
                 last_modified: last_modified.map(CompactString::new),
-                access: counter,
+                access: counter.try_into().unwrap(),
             };
 
             if let Some(mut existing) = self.entries.get_mut(&key) {
@@ -78,7 +78,7 @@ mod inner {
         pub fn get(&self, url: &str) -> Option<(Option<CompactString>, Option<CompactString>)> {
             let key = CompactString::new(url);
             self.entries.get_mut(&key).map(|mut entry| {
-                entry.access = self.access_counter.fetch_add(1, Ordering::Relaxed);
+                entry.access = self.access_counter.fetch_add(1, Ordering::Relaxed).try_into().unwrap();
                 (entry.etag.clone(), entry.last_modified.clone())
             })
         }
--- a/spider/src/utils/hedge.rs
+++ b/spider/src/utils/hedge.rs
@@ -4,7 +4,7 @@
 /// Whichever returns first wins; the loser is cancelled via drop.
 use std::future::Future;
 use std::pin::Pin;
-use std::sync::atomic::{AtomicU64, Ordering};
+use std::sync::atomic::{AtomicUsize, Ordering};
 use std::time::Duration;
 
 /// Configuration for hedged (work-stealing) requests.
@@ -41,22 +41,22 @@ impl Default for HedgeConfig {
 #[derive(Debug)]
 pub struct HedgeTracker {
     /// EMA of page fetch durations in milliseconds.
-    ema_ms: AtomicU64,
+    ema_ms: AtomicUsize,
     /// EMA of squared durations (ms²) for variance/stddev calculation.
-    ema_sq_ms: AtomicU64,
+    ema_sq_ms: AtomicUsize,
     /// Number of duration samples recorded.
-    samples: AtomicU64,
+    samples: AtomicUsize,
     /// Multiplier for the EMA threshold (e.g. 2.0 = hedge if fetch takes >2x average).
     /// Stored as fixed-point: actual * 100 (e.g. 200 = 2.0x).
     threshold_pct: u64,
     /// Minimum samples before adaptive hedging activates.
     min_samples: u64,
     /// Total hedge races fired (delay expired, hedge launched).
-    hedge_fires: AtomicU64,
+    hedge_fires: AtomicUsize,
     /// Hedge races where the hedge tab won.
-    hedge_wins: AtomicU64,
+    hedge_wins: AtomicUsize,
     /// Consecutive retryable errors (5xx, timeout). Reset to 0 on success.
-    consecutive_errors: AtomicU64,
+    consecutive_errors: AtomicUsize,
 }
 
 impl HedgeTracker {
@@ -65,14 +65,14 @@ impl HedgeTracker {
     /// `min_samples` — number of completed fetches before adaptive hedging activates.
     pub fn new(threshold_multiplier: f64, min_samples: u64) -> Self {
         Self {
-            ema_ms: AtomicU64::new(0),
-            ema_sq_ms: AtomicU64::new(0),
-            samples: AtomicU64::new(0),
+            ema_ms: AtomicUsize::new(0),
+            ema_sq_ms: AtomicUsize::new(0),
+            samples: AtomicUsize::new(0),
             threshold_pct: (threshold_multiplier * 100.0) as u64,
             min_samples,
-            hedge_fires: AtomicU64::new(0),
-            hedge_wins: AtomicU64::new(0),
-            consecutive_errors: AtomicU64::new(0),
+            hedge_fires: AtomicUsize::new(0),
+            hedge_wins: AtomicUsize::new(0),
+            consecutive_errors: AtomicUsize::new(0),
         }
     }
 
@@ -211,14 +211,14 @@ impl HedgeTracker {
 impl Clone for HedgeTracker {
     fn clone(&self) -> Self {
         Self {
-            ema_ms: AtomicU64::new(self.ema_ms.load(Ordering::Relaxed)),
-            ema_sq_ms: AtomicU64::new(self.ema_sq_ms.load(Ordering::Relaxed)),
-            samples: AtomicU64::new(self.samples.load(Ordering::Relaxed)),
+            ema_ms: AtomicUsize::new(self.ema_ms.load(Ordering::Relaxed)),
+            ema_sq_ms: AtomicUsize::new(self.ema_sq_ms.load(Ordering::Relaxed)),
+            samples: AtomicUsize::new(self.samples.load(Ordering::Relaxed)),
             threshold_pct: self.threshold_pct,
             min_samples: self.min_samples,
-            hedge_fires: AtomicU64::new(self.hedge_fires.load(Ordering::Relaxed)),
-            hedge_wins: AtomicU64::new(self.hedge_wins.load(Ordering::Relaxed)),
-            consecutive_errors: AtomicU64::new(self.consecutive_errors.load(Ordering::Relaxed)),
+            hedge_fires: AtomicUsize::new(self.hedge_fires.load(Ordering::Relaxed)),
+            hedge_wins: AtomicUsize::new(self.hedge_wins.load(Ordering::Relaxed)),
+            consecutive_errors: AtomicUsize::new(self.consecutive_errors.load(Ordering::Relaxed)),
         }
     }
 }
--- a/spider/src/utils/parallel_backends.rs
+++ b/spider/src/utils/parallel_backends.rs
@@ -2,7 +2,7 @@
 /// alongside the primary crawl path. Lock-free, panic-free, zero overhead when disabled.
 use std::future::Future;
 use std::pin::Pin;
-use std::sync::atomic::{AtomicBool, AtomicU64, AtomicUsize, Ordering};
+use std::sync::atomic::{AtomicBool, AtomicUsize, Ordering};
 use std::time::Duration;
 #[cfg(any(feature = "chrome", feature = "webdriver"))]
 use std::time::Instant;
@@ -196,20 +196,20 @@ pub fn html_quality_score(
 
 /// Per-backend atomic statistics.
 struct BackendStats {
-    wins: AtomicU64,
-    races: AtomicU64,
-    ema_ms: AtomicU64,
-    consecutive_errors: AtomicU64,
+    wins: AtomicUsize,
+    races: AtomicUsize,
+    ema_ms: AtomicUsize,
+    consecutive_errors: AtomicUsize,
     disabled: AtomicBool,
 }
 
 impl BackendStats {
     fn new() -> Self {
         Self {
-            wins: AtomicU64::new(0),
-            races: AtomicU64::new(0),
-            ema_ms: AtomicU64::new(0),
-            consecutive_errors: AtomicU64::new(0),
+            wins: AtomicUsize::new(0),
+            races: AtomicUsize::new(0),
+            ema_ms: AtomicUsize::new(0),
+            consecutive_errors: AtomicUsize::new(0),
             disabled: AtomicBool::new(false),
         }
     }
@@ -218,10 +218,10 @@ impl BackendStats {
 impl Clone for BackendStats {
     fn clone(&self) -> Self {
         Self {
-            wins: AtomicU64::new(self.wins.load(Ordering::Relaxed)),
-            races: AtomicU64::new(self.races.load(Ordering::Relaxed)),
-            ema_ms: AtomicU64::new(self.ema_ms.load(Ordering::Relaxed)),
-            consecutive_errors: AtomicU64::new(self.consecutive_errors.load(Ordering::Relaxed)),
+            wins: AtomicUsize::new(self.wins.load(Ordering::Relaxed)),
+            races: AtomicUsize::new(self.races.load(Ordering::Relaxed)),
+            ema_ms: AtomicUsize::new(self.ema_ms.load(Ordering::Relaxed)),
+            consecutive_errors: AtomicUsize::new(self.consecutive_errors.load(Ordering::Relaxed)),
             disabled: AtomicBool::new(self.disabled.load(Ordering::Relaxed)),
         }
     }
--- a/spider/src/utils/rate_limiter.rs
+++ b/spider/src/utils/rate_limiter.rs
@@ -7,7 +7,7 @@
 mod inner {
     use crate::compact_str::CompactString;
     use dashmap::DashMap;
-    use std::sync::atomic::{AtomicU64, Ordering};
+    use std::sync::atomic::{AtomicUsize, Ordering};
     use std::time::{Duration, Instant};
 
     /// Maximum number of tracked domains before LRU eviction kicks in.
@@ -77,7 +77,7 @@ mod inner {
         default_rate: f64,
         default_burst: u32,
         /// Monotonically increasing counter for LRU tracking.
-        access_counter: AtomicU64,
+        access_counter: AtomicUsize,
     }
 
     impl DomainRateLimiter {
@@ -90,7 +90,7 @@ mod inner {
                 buckets: DashMap::with_capacity(64),
                 default_rate: default_rate.clamp(0.01, 1_000_000.0),
                 default_burst: default_burst.clamp(1, 10_000),
-                access_counter: AtomicU64::new(0),
+                access_counter: AtomicUsize::new(0),
             }
         }
 
@@ -102,14 +102,14 @@ mod inner {
 
             // Fast path: domain already tracked.
             if let Some(mut bucket) = self.buckets.get_mut(&key) {
-                bucket.last_access = counter;
+                bucket.last_access = counter.try_into().unwrap();
                 return bucket.try_acquire();
             }
 
             // Slow path: insert new bucket, evicting if over capacity.
             self.maybe_evict();
 
-            let mut bucket = TokenBucket::new(self.default_rate, self.default_burst, counter);
+            let mut bucket = TokenBucket::new(self.default_rate, self.default_burst, counter.try_into().unwrap());
             let wait = bucket.try_acquire();
             self.buckets.insert(key, bucket);
             wait
--- a/spider/src/utils/warc.rs
+++ b/spider/src/utils/warc.rs
@@ -16,7 +16,7 @@ mod inner {
     use crate::page::Page;
     use std::io::{self, Write};
     use std::path::{Path, PathBuf};
-    use std::sync::atomic::{AtomicU64, Ordering};
+    use std::sync::atomic::{AtomicUsize, Ordering};
     use tokio::sync::{broadcast, mpsc};
 
     /// CRLF constant.
@@ -60,7 +60,7 @@ mod inner {
         #[cfg(not(feature = "spoof"))]
         {
             use std::hash::{Hash, Hasher};
-            static CTR: AtomicU64 = AtomicU64::new(0);
+            static CTR: AtomicUsize = AtomicUsize::new(0);
             let mut hasher = ahash::AHasher::default();
             CTR.fetch_add(1, Ordering::Relaxed).hash(&mut hasher);
             std::thread::current().id().hash(&mut hasher);
@@ -248,7 +248,7 @@ mod inner {
     #[derive(Clone)]
     pub struct WarcWriter {
         tx: mpsc::UnboundedSender<Vec<u8>>,
-        record_count: std::sync::Arc<AtomicU64>,
+        record_count: std::sync::Arc<AtomicUsize>,
         path: std::sync::Arc<PathBuf>,
     }
 
@@ -271,7 +271,7 @@ mod inner {
 
             let file = std::fs::File::create(&path)?;
             let (tx, rx) = mpsc::unbounded_channel::<Vec<u8>>();
-            let record_count = std::sync::Arc::new(AtomicU64::new(0));
+            let record_count = std::sync::Arc::new(AtomicUsize::new(0));
 
             let writer = Self {
                 tx,
@@ -322,7 +322,7 @@ mod inner {
 
         /// Number of records written so far.
         pub fn record_count(&self) -> u64 {
-            self.record_count.load(Ordering::Relaxed)
+            self.record_count.load(Ordering::Relaxed).try_into().unwrap()
         }
 
         /// The output file path.
