--- a/spider/Cargo.toml
+++ b/spider/Cargo.toml
@@ -118,7 +118,8 @@ features = ["serde"]
 
 [target.'cfg(target_os = "linux")'.dependencies]
 tokio-uring = { version = "0.4", optional = true }
-libc = "0.2"
+# musl+getrandom
+libc = "=0.2.172"
 
 [target.'cfg(not(target_arch = "wasm32"))'.dependencies]
 tokio = { version = "1", default-features = false, features = [
